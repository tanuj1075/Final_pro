{
  "name": "final-pro-monorepo",
  "private": true,
  "scripts": {
    "dev": "echo 'Run this project via a PHP server (e.g. php -S localhost:8000 api/index.php)'"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
